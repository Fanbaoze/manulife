# 中宏风格年金险计算器 & KOC客情分析系统 —— 纯网页版部署指南

这是你原来 AI Studio 里那个网页应用，**代码结构完全没动**（组件、精算引擎、图表都是原样），
唯一改的是 `server.ts` 里两个 AI 接口，把直连 Gemini 换成了走豆包（火山方舟），因为 Gemini 国内访问不通。
部署完你会拿到一个网址（类似 `https://xxx.onrender.com`），谁要用直接把网址发给他就行，不用装小程序、不用审核。

## 一、为什么不用管 ICP 备案

网站托管在国外（下面推荐的 Render 平台服务器在海外），不涉及在中国大陆境内经营网站，所以不需要 ICP 备案，今天就能上线。
代价是：国内同事打开可能会比国内网站略慢一点，Render 的免费套餐如果长时间没人访问会"休眠"，
休眠后第一个人打开需要等待约 30 秒唤醒，之后就正常了——对内部几个同事用完全够用。

如果链接是在微信聊天里点开的，微信会用自带浏览器打开，一般能正常访问；如果某次弹出"网页可能存在风险"之类的提示，
点"继续访问"就行，这是微信对不熟悉域名的常规风险提示，不代表网站有问题。

## 二、部署步骤（用 Render，全程网页操作，不用命令行）

1. **注册 GitHub 账号**（如果还没有）：去 github.com 免费注册。
2. **新建一个仓库**：右上角 "+" → New repository，随便起个名字（比如 `koc-annuity-tool`），设为 Private（私有）即可，点 Create。
3. **把这个项目上传上去**：进入刚建好的空仓库页面，会看到 "uploading an existing file" 的链接，点它，
   把这个压缩包解压后的**所有文件**（整个文件夹的内容，不是压缩包本身）拖进去上传，底部填个提交说明，点 Commit changes。
   （`node_modules` 文件夹本来就没有，不用担心漏传，`.gitignore` 已经排除了不该传的东西。）
4. **注册 Render 账号**：去 render.com，用 GitHub 账号一键登录最方便，这样能直接看到你刚建的仓库。
5. **新建 Web Service**：点 New → Web Service → 选中刚才那个仓库 → Connect。
6. **填几个配置项**：
   - Build Command 填：`npm install && npm run build`
   - Start Command 填：`npm start`
   - Instance Type 选 **Free**
7. **加环境变量**（在同一个创建页面往下滑，或者创建完在 Environment 标签页加）：
   - `ARK_API_KEY` = 你在火山方舟控制台生成的 API Key
   - `ARK_MODEL` = 你创建的推理接入点 ID
   - `NODE_ENV` = `production`
   - 还没申请豆包 Key 也没关系，先不填这两个，两个 AI 功能会自动走内置的模拟数据，界面照样能用，之后申请到了回来补填、点 "Manual Deploy" 重新部署一次就行。
8. **点 Create Web Service**，等 3-5 分钟看它跑完构建日志，成功后页面顶部会显示一个 `https://你的项目名.onrender.com` 的网址，这就是最终地址。

## 三、以后要更新代码怎么办

以后如果要改代码（比如我这边再帮你调整），把改动后的文件重新拖进 GitHub 仓库覆盖上传一次，
Render 检测到仓库有更新会自动重新构建部署，不用重新走一遍上面的步骤。

## 四、和原来 AI Studio 预览版比

功能、界面、精算逻辑完全一样，唯一区别是 AI 现在走的是豆包而不是 Gemini（国内能访问通了），
其余一切都是你原来看到的那个网页，没有任何删减。
